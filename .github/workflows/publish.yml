name: Publish to PyPI

"on":
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint and docs
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Use python 3.13
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Install
        run: python -m pip install -r requirements-dev.txt ; python -m pip install -r requirements.txt

      - name: Lint
        run: make lint

      - name: Mutable class attribute check
        run: python ./scripts/check_mutable_class_attrs.py

      - name: Docs
        run: make docs

  pretest:
    needs: lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.14"]
    name: Pre-publish tests (Python ${{ matrix.python }})
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Use python ${{ matrix.python }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Install
        run: python -m pip install -r requirements-dev.txt ; python -m pip install -r requirements.txt

      - name: Start devenv
        run: |
          set -euo pipefail
          if docker compose -f ./devenv/ci.yaml up -d --wait; then
            echo "compose --wait succeeded"
          else
            echo "compose --wait unsupported or failed, falling back to health poll"
            docker compose -f ./devenv/ci.yaml up -d
            ok=0
            for i in $(seq 1 40); do
              if python ./scripts/check_devenv_healthy.py; then
                ok=1
                break
              fi
              sleep 3
            done
            if [ "$ok" != 1 ]; then
              echo "devenv health poll timed out"
              docker compose -f ./devenv/ci.yaml ps
              exit 1
            fi
          fi
          python ./scripts/check_devenv_healthy.py
          docker compose -f ./devenv/ci.yaml ps

      - name: Mutable class attribute check
        run: python ./scripts/check_mutable_class_attrs.py

      - name: Test with coverage
        env:
          PYPEPPER_REQUIRE_DEVENV: "1"
        run: pytest --cov=pypepper --cov-report=term --cov-fail-under=90 tests/

      - name: Stop devenv
        if: always()
        run: docker compose -f ./devenv/ci.yaml down

  publish:
    name: Build and publish
    needs: pretest
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Use python 3.13
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Verify tag matches pyproject version
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME#v}"
          PKG_VERSION="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml","rb"))["project"]["version"])')"
          echo "tag=${TAG} pyproject=${PKG_VERSION}"
          if [ "${TAG}" != "${PKG_VERSION}" ]; then
            echo "::error::Git tag v${TAG} does not match pyproject.toml version ${PKG_VERSION}"
            exit 1
          fi

      - name: Install build
        run: python -m pip install --upgrade pip build

      - name: Build
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
